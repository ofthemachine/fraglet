#!/usr/bin/env -S fragletc --vein=ocaml
(* OCaml: where if it compiles, it's probably correct. Types are documentation that never lies. *)
let () = print_endline "Hello from OCaml!"
let () = print_endline "ML's disciplined sibling. No runtime surprises, only compile-time ones."
